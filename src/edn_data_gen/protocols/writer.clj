(ns edn-data-gen.protocols.writer
  "Writer protocol")

;; (defprotocol IWriter
;;   "Protocol for writers"
;;   (append [writer c] "Append a character.")
;;   (write [writer s] "Write a string.")
;;   (flush [writer] "Flush the stream.")
;;   (close [writer] "Close the stream."))
