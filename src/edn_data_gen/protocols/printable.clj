(ns edn-data-gen.protocols.printable
  "Print protocol.")

;; (defprotocol IPrintable
;;   "Protocol for printing to a writer via the IWriter protocol."
;;   (print [this writer opts]
;;     "Print this using writer."))
